
# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
tsconfig.tsbuildinfo

# production
/build

# tina generated artifacts (rebuilt by `tinacms build`/`tinacms dev`)

# Client content + assets are no longer committed here: scripts/fetch-content.mjs
# pulls them fresh from the public client site repo at container start (see
# Dockerfile ENTRYPOINT), so the image never bakes in stale content.
/src/clients/blessing-marketing-od

# misc
.DS_Store
.env
.env.local
.idea

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
